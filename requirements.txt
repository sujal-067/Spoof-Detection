# Smart Depth Vision — requirements.txt
# Install with: pip install -r requirements.txt
#
# IMPORTANT — install PyTorch FIRST from https://pytorch.org/get-started/locally/
# CPU-only example (Windows/Linux):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# CUDA 12.1 example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9         # required for UploadFile / multipart forms

# ── Auth & Security ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4             # password hashing

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30

# ── Computer Vision & Numerics ────────────────────────────────────────────────
# Use opencv-python if running locally (GUI support); headless for servers
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4
pyyaml==6.0.1

# ── Deep Learning (torch/torchvision installed separately — see above) ─────────
ultralytics==8.2.0               # YOLOv8 — auto-downloads yolov8s.pt on first run
timm==0.9.16                     # required by MiDaS DPT models
# MiDaS is loaded via torch.hub — no pip package needed

# ── CLIP ─────────────────────────────────────────────────────────────────────
# The official OpenAI package is not on PyPI; install from source:
#   pip install git+https://github.com/openai/CLIP.git
#
# Alternatively, the unofficial mirror works on most machines:
#   pip install openai-clip==1.0.1
#
# Dependencies that both packages share (safe to install via pip):
ftfy==6.1.3
regex==2024.5.15

# ── Face Recognition ─────────────────────────────────────────────────────────
deepface==0.0.91
# DeepFace auto-downloads Facenet weights (~90 MB) on first extract_embedding() call.
# Also pulls in: tensorflow or tf-keras, keras, mtcnn, retina-face

# ── Utilities ────────────────────────────────────────────────────────────────
scipy==1.13.0                    # used by deepface distance calculations
tqdm==4.66.4
requests==2.32.3